#include <stdio.h>

int main(int argc, char const *argv[])
{
    char cname[80], cnumb[10];
    int preread, curread;

    printf("Customer Name: ");scanf("%s", &cname);
    printf("Customer Number: ");scanf("%s", &cnumb);
    printf("Previous reading: ");scanf("%s", &preread);
    printf("Current reading: ");scanf("%s", &curread);

    // Question incomplete
    return 0;
}
